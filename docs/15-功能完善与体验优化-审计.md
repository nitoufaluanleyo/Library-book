# 图书馆座位预约系统功能完善与体验优化审计

> 审计日期：2026-07-13
>
> 审计对象：`README.md`、`docs/15-功能完善与体验优化记录.md`、`docs/项目任务板与迭代记录.md`、`src/`
>
> 对照基线：`docs/13-用户端主链路开发记录.md`、`docs/14-管理端与权限开发记录.md`、前序审计结果和任务板
>
> 审计边界：只审阶段 15 新增结果；前序材料只作为缺口来源，不重做阶段 14 或更早阶段的全量审计

## A. 总体判断

**审计结论：不通过，但本轮缺口收束、任务分区和未越级处理正确。**

当前不存在 `src/`、Solution 或 Web 项目，阶段 15 没有产生功能、页面、校验、提示、样式或数据代码。关键次要功能、异常处理、统一状态、空状态、反馈信息和响应式体验均未实现，不能达到功能完善阶段最低完成线。

docs/15 汇总的缺口均来自前序页面卡、开发记录、审计和任务板，没有临时扩展支付、扫码、消息推送等无关功能。记录还正确识别 Sprint 3/T15 属于管理端闭环，状态一致性、体验整合和全链路验收主要属于 Sprint 4/T16，没有擅自修改任务范围或虚报完成。

README、docs/15 和任务板均明确记录阶段完成度 0%、没有 build/run、不能进入下一阶段。这部分台账真实。

## B. 发现的问题清单

### B1. P0：没有阶段 15 代码产物

实际检查结果：

- `src/` 不存在；
- `src/LibrarySeatReservation.Web/` 不存在；
- `LibrarySeatReservation.sln` 不存在。

因此没有可核验的 MVC 页面、业务 Service、校验、状态组件、反馈 Partial、自定义样式或数据库分支。阶段 15 当前只完成了缺口整理，没有完成功能完善。

### B2. P0：关键次要功能未补齐

以下已确认功能仍未实现：

- 体验账号 Session 切换；
- 用户预约提交、回看和取消；
- 管理员登录、预约处理和座位维护；
- 五项数据库统计；
- 用户端与管理端状态回流。

这些不是本轮新增范围，而是前序阶段尚未完成的课程核心能力。缺少它们时无法进入体验收束。

### B3. P0：关键异常没有代码处理

当前没有预约 Service、Controller、数据库约束或错误映射，无法验证：

- SeatConflict 和 UserConflict 两类重复预约；
- 停用座位不可预约；
- 越权取消、开始后取消和重复取消；
- RowVersion 并发变化；
- 数据库失败时的安全提示；
- 表单无效输入和重复提交。

docs/15 只将这些列为待办，不能视为已处理。

### B4. P0：状态显示未统一

Reserved、Cancelled、Completed 尚未形成统一 Service 规则或共享状态组件。U-P05、A-P02 和 A-P04 没有可运行页面，无法验证存储状态、动态展示状态、取消权限和统计口径一致。

### B5. P0：空状态和提示信息未补齐

当前没有真实数据分支或 Razor 页面，无法验证：

- 空座位列表；
- 空预约记录；
- 后台筛选无结果；
- 空统计显示 0；
- 预约、取消、登录、座位维护的成功或失败反馈；
- 刷新后不重复提交。

静态原型中的展示效果不等于真实空状态和反馈逻辑。

### B6. P0：手机端和桌面端体验没有代码级改进

当前没有可运行用户端或管理端页面，无法在 390px 手机视口和 1366px 桌面视口执行回归。用户端操作区、管理端筛选表格、状态标签、横向滚动和响应式导航均没有本轮代码改动。

### B7. 通过项：缺口优先级来自既有材料

docs/15 优先列出工程、用户闭环、管理闭环、异常、状态、空状态、提示和响应式问题，并明确“没有可运行功能”优先于样式细节。该顺序来自前序审计和任务板，没有新增无关功能，符合“先补关键缺口，再做样式细节”的要求。

### B8. 通过项：默认补足和假设记录清楚

docs/15 已单列“默认补足项 / 当前假设”，明确本轮未使用默认补足；异常类型、状态名称、空状态、提示和响应式目标均来自前序材料。该说明与实际无代码结果一致。

### B9. 通过项：任务板记录真实

任务板真实记录了：

1. Sprint 3/T15 仍是管理端闭环，7 张卡片全部待开发，完成率 0%。
2. Sprint 4/T16 承接统计、状态一致性、9 页体验和全链路验收，7 张卡片全部待开发，完成率 0%。
3. 本轮没有卡片进入“进行中”“待验证”或“已完成”，没有伪造状态流转。
4. Sprint 3 站会、评审和回顾均记录工程不存在、主链路未实现和未执行 build/run。
5. 评审结论为 Sprint 3 未开工，不允许提前推进 T16。

用户要求检查 Sprint 3 记录，本轮记录是真实的；同时它正确指出功能完善并不等于改写 T15 管理端任务。

### B10. 通过项：README 已按真实结果增量更新

README 已明确：

- 阶段 15 功能完善尚未开工；
- 当前没有可运行主链路；
- 重复预约、停用座位拦截、非法取消、统一状态、真实空状态、反馈和响应式页面均不可验证；
- 静态原型不能替代业务验收；
- 阶段 12 至阶段 15 均未进入代码实现。

后续运行命令和计划能力仍明确标注为待完成，没有被误写为当前已存在，也无需删除。

### B11. 通过项：阶段结论没有越级

docs/15 明确写出功能完善阶段完成度 0%，三项最低完成线均未达到，并判定不能进入“联调、测试与缺陷闭环”。该结论与任务板一致。

### B12. 审计边界符合要求

本轮只引用阶段 13、阶段 14 的 0% 结果及任务板 T15/T16 分工作为阶段 15 的直接证据，没有重新审计前序文档全部内容。未发现需要在本审计中扩大处理的非阻塞历史问题。

## C. 必改项

1. **先形成可运行主链路。** 完成工程骨架、数据库、用户端和管理端闭环，并取得 build、run、真实查写库和权限验收证据。
2. **落实异常处理。** 在 Service 和数据库层实现两类预约冲突、停用座位、非法取消、并发变化和数据失败处理，并在页面显示安全反馈。
3. **统一状态规则。** U-P05、A-P02、A-P04 必须共用 Reserved、Cancelled、Completed 的计算和展示口径。
4. **补齐真实空状态和提示。** 使用真实查询结果触发空座位、空预约、空筛选和空统计，并验证成功、失败、重复提交提示。
5. **完成响应式回归。** 用户端按 390px 手机视口、管理端按 1366px 桌面视口验证布局、表格、操作区和导航。
6. **按任务板逐卡验收。** 先关闭 T13-T15，再执行 T16-02、T16-04 和 T16-03；每张卡保留 build/run、浏览器和数据库证据。

## D. 建议优化项

1. 功能恢复后先验证异常和状态一致性，再处理颜色、间距等样式细节。
2. 为常见业务结果使用统一反馈组件，避免每个页面各写一套提示文案和样式。
3. 空状态应由真实空查询触发，并提供返回主链路的明确操作，不使用纯说明文字占位。
4. 响应式验收保留用户端和管理端截图，并检查横向溢出、固定操作列和底部安全留白。
5. README 当前状态准确；实际完成卡片后再增量更新已实现范围和限制变化。

## E. 是否可以进入下一步“联调、测试与缺陷闭环”

**不可以。**

当前没有可运行工程或业务链路，功能完善阶段完成度为 0%。关键功能、异常处理、统一状态、空状态、反馈和响应式体验均未实现。项目应继续停留在阶段 15 的前置处理中；先完成工程和 T13-T15 主链路，再按 T16 逐轮完成体验收束并重新申请阶段放行。

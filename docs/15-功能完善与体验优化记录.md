# 图书馆座位预约系统功能完善与体验优化记录

## 0. 本轮开工结论

执行日期：2026-07-13。

**本轮未进入功能完善或体验优化代码开发。**

开始前检查发现：

1. `docs/14-管理端与权限开发-审计.md` 明确判定不能进入“功能完善、异常与体验优化”。
2. 阶段 13 用户端和阶段 14 管理端完成度均为 0%，不存在可回归的主链路。
3. `LibrarySeatReservation.sln`、`src/LibrarySeatReservation.Web/` 和 Web `.csproj` 均不存在，工程不能 build 或 run。
4. 任务板中的 Sprint 3/T15 是管理端闭环，不是功能完善分区；体验整合、状态一致性、空错状态和全链路验收主要位于 Sprint 4/T16-02 至 T16-04。

按照既有任务板优先的规则，本轮不把 T15 管理端任务改写成体验优化任务，也不在没有业务代码的情况下创建表面样式或虚构异常处理结果。

## 1. 已确认但尚未完成的缺口

### 1.1 工程与数据前提

- Solution、Web 项目、EF Core、LocalDB、Migration 和种子数据均不存在。
- build、run、真实查库、真实写库均未发生。
- 当前没有可用于回归的用户端或管理端入口。

### 1.2 用户端缺口

- 体验用户 Session、首页真实数据、座位列表和详情尚未实现。
- U-P04 预约提交、冲突校验、停用座位拦截和表单验证尚未实现。
- U-P05 我的预约、数据隔离、非法取消提示和时段释放尚未实现。
- Reserved、Cancelled、Completed 的状态显示尚未形成代码级统一规则。

### 1.3 管理端缺口

- 管理员哈希登录、AdminCookie、后台访问保护和退出尚未实现。
- A-P02 日期/状态筛选、管理员取消和状态回流尚未实现。
- A-P03 座位新增、编辑、启停和编号唯一校验尚未实现。
- A-P04 五项统计没有数据库数据来源。

### 1.4 体验与异常缺口

前序页面卡和审计已经要求以下效果，但当前只能在静态原型中看到外观，不能视为业务实现：

1. 两类重复预约冲突提示。
2. 已停用座位不可预约。
3. 越权、已开始、重复取消和并发变化提示。
4. 用户端与管理端统一状态标签。
5. 空座位、空预约、空筛选结果和空统计呈现。
6. 成功或失败反馈以及刷新后不重复提交。
7. 用户端 390px 手机布局和管理端 1366px 后台布局。

当前关键缺口是“没有可运行功能”，优先级高于样式细节。

## 2. 本轮任务卡清单

### 2.1 任务板定位结果

| 分区 | 已确认范围 | 当前状态 |
| --- | --- | --- |
| Sprint 3 / T15-01 至 T15-07 | 管理端认证、预约管理、座位管理和验收 | 7 张均待开发 |
| Sprint 4 / T16-01 至 T16-07 | 统计、状态一致性、9 页体验整合、全链路验收和交付 | 7 张均待开发 |

### 2.2 本轮任务选择

| 卡片 | 本轮状态 | 是否验证通过 | 说明 |
| --- | --- | --- | --- |
| 无 | 未选择 | 否 | 工程、用户端和管理端均未实现，没有可供完善或回归的功能 |

### 2.3 本轮选择理由

功能完善应建立在稳定主链路之上。本轮即使选择 T16-02、T16-03 或 T16-04，也没有状态规则、9 个 Razor 页面或可执行全链路可以验证；选择 T15 卡片又会把管理端开发与体验优化混为一谈。因此本轮不推进任何卡片状态。

## 3. 本轮修改点清单

| 分类 | 本轮修改 | 结果 |
| --- | --- | --- |
| 功能 | 无 | 未补次要功能 |
| 页面 | 无 | 未创建或修改 Razor 页面 |
| 校验 | 无 | 未创建表单或业务校验 |
| 提示 | 无 | 未创建成功、失败或异常反馈 |
| 样式 | 无 | 未修改代码目录样式 |
| 数据 | 无 | 未查库、写库或修改种子数据 |
| README | 增加阶段 15、功能完善真实状态和限制说明 | 已完成文档检查 |
| 任务板 | 补充 Sprint 3 前置核验、站会、评审和回顾 | 已完成文档检查 |

## 4. 本轮验证结果

| 验证项 | 结果 | 证据或原因 |
| --- | --- | --- |
| 用户端主链路 | 不通过 | 阶段 13 完成度 0% |
| 管理端主链路 | 不通过 | 阶段 14 完成度 0% |
| Solution / Web 项目 | 不通过 | `.sln`、`src` 和 `.csproj` 不存在 |
| `dotnet build` | 未执行 | 没有可构建工程 |
| `dotnet run` | 未执行 | 没有可启动工程 |
| 冲突与停用座位 | 未验证 | 没有预约 Service 和数据库 |
| 非法取消 | 未验证 | 没有用户或管理员取消动作 |
| 状态显示统一 | 未验证 | 没有 Razor 页面和统一状态规则实现 |
| 空状态与提示 | 未验证 | 只有静态原型，没有真实数据分支 |
| 响应式体验 | 未验证 | 没有可运行页面可做浏览器回归 |
| Markdown 状态一致性 | 通过 | README、docs/15 和任务板均未标记 T15/T16 完成 |

本轮没有修改代码和原型，因此没有破坏既有产物；但“工程保持可 build、可 run”仍无法达成。

## 5. 当前任务统计

### 5.1 Sprint 3 管理端闭环

| 指标 | 当前值 |
| --- | --- |
| 任务总数 | 7 |
| 已完成 | 0 |
| 进行中 | 0 |
| 待验证 | 0 |
| 待开发 | 7 |
| 本轮新增完成卡片 | 无 |
| 当前完成率 | 0%（0/7） |

### 5.2 Sprint 4 统计、联调与交付

| 指标 | 当前值 |
| --- | --- |
| 任务总数 | 7 |
| 已完成 | 0 |
| 进行中 | 0 |
| 待验证 | 0 |
| 待开发 | 7 |
| 本轮新增完成卡片 | 无 |
| 当前完成率 | 0%（0/7） |

## 6. 当前阶段完成度判断

功能完善与体验优化阶段当前完成度为 **0%**。本阶段最低完成线均未达到：

- 次要功能补齐到课程要求：未完成；
- 关键异常处理：未完成；
- 空状态、提示信息和体验细节：未完成。

当前不能进入“联调、测试与缺陷闭环”。

## 7. 下一轮优先任务建议

1. 先关闭开发前 P0，完成阶段 12 工程骨架和数据库初始化。
2. 按 T13、T14 完成用户浏览和预约闭环，取得真实查写库与异常分支证据。
3. 按 T15 完成管理员认证、预约处理、座位维护和状态回流。
4. 主链路通过后先执行 T16-02 状态一致性，再执行 T16-04 的权限、冲突、空错状态全链路验收。
5. 功能和异常稳定后再执行 T16-03 响应式与视觉整合，避免样式先行掩盖业务缺口。

## 8. 默认补足项 / 当前假设

本轮未使用默认补足。异常类型、状态名称、空状态、提示和响应式目标均已在前序页面卡、开发记录、审计和任务板中明确。

## 9. Git 说明

本轮只提交 README、任务板和本记录文档。由于没有工程，最小自检采用 Markdown 差异检查、任务编号检查和状态一致性检查，不声称完成 `dotnet build` 或 `dotnet run`。远端已配置；文档检查通过后推送当前提交。

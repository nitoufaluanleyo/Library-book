# 图书馆座位预约系统开发起步与骨架记录

## 0. 开工门禁阻塞说明

检查日期：2026-07-13。

本阶段在生成代码前，已先检查 `docs/11-开发前一致性总审计.md` 及其最新复核结果 `docs/11-开发前一致性总审计-复核.md`。

虽然 `docs/11-开发前一致性总审计.md` 中写有“P0 当前剩余数量：0”和“结论：Go”，但后续 11b 复核已经明确给出：

- 复核不通过；
- 当前结论为 No-Go；
- 必须退回 11a 继续修订；
- 修订完成并再次确认 P0 清零前，不得进入“开发起步与项目骨架”。

最新复核结论是当前有效门禁。因此，本轮停止生成代码，不创建 `.sln`、`.csproj`、`src/`、`Migrations/` 或 LocalDB 数据库，也不执行 `dotnet new`、`dotnet build`、`dotnet run`、`dotnet ef migrations add` 和 `dotnet ef database update`。

本次阻塞与工程文件尚未生成无关，原因是开发前文档仍存在会影响页面职责和代码契约的未关闭 P0。

## 1. 当前未关闭的 P0

### 1.1 CONS-01：U-P03 与 U-P04 页面职责仍有旧口径

- `docs/03-PRD-Lite.md` 仍有“在座位详情页判断或选择日期、时段”的表述。
- 同一文档又规定 U-P03 只显示座位详情，U-P04 负责日期、时段、占用状态和预约提交。
- “不生成独立的座位详情”与固定 9 页中的 U-P03 直接冲突。

解除条件：统一为 U-P03 只负责座位详情，U-P04 负责日期、时段、两类占用状态和提交。

### 1.2 CONS-07：数据库查询用途仍指向错误页面

- `docs/08-数据库设计.md` 仍将预约时段索引描述为“座位详情的时段占用查询”。
- 该查询按已确认页面职责应服务于 U-P04 预约提交页。

解除条件：修订索引用途说明，使其与 U-P04 查询职责一致。

### 1.3 CONS-11：ReservationCreateViewModel 工程契约仍冲突

- `docs/07-系统设计说明.md` 将 `ReservationCreateViewModel` 定义为包含页面展示数据和提交字段的完整页面模型。
- `docs/09-关键链路详细设计.md` 又写该 ViewModel “只包含 SeatId、ReservationDate、TimeSlot”。

解除条件：保留完整页面 ViewModel，并明确 POST 只绑定 `SeatId`、`ReservationDate`、`TimeSlot`；`ExperienceUserId` 必须从 Session 获取，不能由表单提交。

## 2. 其他待修订项

以下问题当前按 P1 管理，不单独构成本次 No-Go 的核心原因，但应由 11a 一并修订并交给 11b 复核：

1. `CONS-05`：用户端底部导航在 docs/05、docs/06 和 `prototype/static-v2` 中仍不一致，不能标记为“已复核”。
2. AT01 与 M1 映射：`docs/11` 的 `T12-02 至 T12-10` 与任务板 M1 的 `T12-02`、`T12-03`、`T12-05` 至 `T12-11` 不一致。
3. `CONS-13`：仓库根 README 入口仍待在 Sprint 0 冒烟验收前处理；该项不阻塞业务代码开工。

## 3. 本轮执行结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 开发门禁 | 未通过 | 11b 明确给出 No-Go |
| Solution / Web 项目 | 未创建 | 未执行 `dotnet new` |
| EF Core 依赖 | 未接入 | 无 `.csproj` 可配置 |
| Entity / Service / DataAccess 骨架 | 未生成 | 遵守门禁停止代码生成 |
| 首次 Migration | 未创建 | 未进入 Code First 实施 |
| LocalDB 建库建表 | 未执行 | 未执行数据库更新 |
| 最小种子数据 | 未生成 | 未进入数据初始化阶段 |
| README 工程状态 | 未更新 | 不把未生成内容写成已完成事实 |
| Sprint 0 任务卡 | 保持未完成 | T12-02、T12-03、T12-05 至 T12-11 均未验收 |

## 4. 解除阻塞条件

只有同时满足以下条件，才能重新启动阶段 12：

1. 11a 按最新复核意见修订 `docs/03`、`docs/08`、`docs/09`、`docs/11`、README 和任务板中的对应内容。
2. `CONS-01`、`CONS-07`、`CONS-11` 的源文档冲突已消失，任务板状态与实际修订结果一致。
3. 11b 再次复核并明确写出“P0 已清零”且“可以进入开发起步与项目骨架”。
4. 任务板将开发前门禁状态更新为“已复核”。

## 5. 重新开工后的执行顺序

门禁解除后，继续沿用当前 Sprint 0 任务卡，不重新发明范围：

1. T12-02：创建 `LibrarySeatReservation.sln`。
2. T12-03：创建 `src/LibrarySeatReservation.Web` MVC 项目并加入 Solution。
3. T12-05：接入 EF Core SQL Server、Design/Tools 依赖并完成基础注册。
4. T12-06 至 T12-07：完成首次 build 和 run。
5. T12-08 至 T12-09：创建首次 Migration、更新 LocalDB 并初始化最小种子数据。
6. T12-10：完成 Sprint 0 冒烟验收并按实测结果增量更新 README。

## 6. 默认补足项 / 当前假设

本轮未进入骨架设计和代码实现，没有新增实体、目录、路由、接口或数据库假设。阻塞项及解除条件全部来自现有文档和最新 11b 复核结论。
